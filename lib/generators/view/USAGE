Description:
    Generates views for existing model passed as argument

Example:
    rails generate view Thing

    This will create:
        7 CRUD views for Thing model, based on Thing accessible
        attributes and on default template engine

